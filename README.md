DMX512
===========================
Node server and clientside DMX dummy

to start this thing:
`cd app && npm start`
`cd server && npm start`


to build this thing:
`cd server && npm install`
`cd app && npm install`
